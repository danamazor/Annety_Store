@model IEnumerable<Annety.ProductSize>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<div class="col-md-4">
    <p class="col-md-4">@Html.ActionLink("Create New", "Create")</p>
    <p class="col-md-7">@Html.ActionLink("Back to Manage Page", "Manage", "Home") </p>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sequence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SizeDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sequence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SizeDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodeSize }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodeSize }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodeSize })
        </td>
    </tr>
}

</table>
